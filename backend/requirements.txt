# Quantara AI — Python 3.14.2 compatible dependencies
# Install: python3.14 -m pip install -r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# Auth — PyJWT works on Python 3.14, no C extensions needed
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20

# Database — aiosqlite (zero config SQLite, pure Python)
sqlalchemy==2.0.36
aiosqlite==0.20.0
# asyncpg==0.30.0  # Uncomment for PostgreSQL (needs libpq)

# HTTP client (for optional OpenAI calls)
httpx==0.28.1

# Utils
python-dotenv==1.0.1

# NOTE: numpy removed — ML engine is pure Python, no C compiler needed
